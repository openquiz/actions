name: "Issue Action"
on:
  issues:
    types: [labeled]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - uses: ./
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"