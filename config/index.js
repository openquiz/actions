const project = {
  owner: 'openquiz',
  repo: 'openquiz',
  patchBranch: 'bot-patch',
  masterBranch: 'master'
}

const labels = {
  quiz: 'quiz',
  titleRequest: 'title request',
  ready: 'READY'
}

const administrator = [
  'leplay'
]

const editors = {
  friends: []
}

module.exports = {
  project,
  labels,
  administrator,
  editors
}
