const project = {
  owner: 'openquiz',
  repo: 'openquiz'
}

const labels = {
  quiz: 'quiz',
  titleRequest: 'title request'
}

const administrator = [
  'leplay'
]

const editors = {
  friends: []
}

module.exports = {
  project,
  labels,
  administrator,
  editors
}
